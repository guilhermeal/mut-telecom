CLAUDE.md

# SO / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Logs
*.log

# Ambiente local (credenciais reais — nunca commitar; use .env.example como template)
.env
.env.local

# Dados de runtime do formulário de contato (rate limiting) — não é código, não vai pro git
/storage/*
!/storage/.gitkeep
